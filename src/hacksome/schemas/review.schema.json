{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "decision": {"type": "string", "enum": ["pass", "reject"]},
    "markdown": {"type": "string", "minLength": 1}
  },
  "required": ["decision", "markdown"]
}
