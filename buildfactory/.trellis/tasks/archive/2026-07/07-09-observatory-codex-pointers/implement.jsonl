{"file": "observatory/runner.py", "reason": "_pointers_dataplane (around line 228) hardcodes the claude transcript layout `sessions/<role>/projects/**/*.jsonl`; add the codex layout `sessions/<role>/codex/sessions/**/*.jsonl` with a one-line provider discriminator (agents/<role>.yaml `provider`). Pointers only — never read for the observer, never mention the report dir."}
{"file": "observatory/charters/goal_postmortem.md", "reason": "De-claude the wording ('恢复各自的 claude session' → runtime-neutral) and make the tool-trail investigation instruction (question 2, 数 tool_use) format-agnostic — codex rollouts have different event shapes; the observer reads repo code to derive them."}
{"file": "observatory/charters/company_review.md", "reason": "Same runtime-neutral wording fix (line 7 mentions claude session); transcript references must not presume claude event format."}
{"file": "agent/runtimes/codex.py", "reason": "Ground truth for the codex home layout: CODEX_HOME = sessions/<role>/codex, rollouts under its sessions/ subdir (materialize_home docstring + home_env)."}
{"file": "observatory/tests/test_runner.py", "reason": "Existing tests may assert pointer text — update alongside; add an assertion that both layouts appear in A/B prompts."}
