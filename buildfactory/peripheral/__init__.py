"""Foundagent peripheral layer — turn external-world async notifications into a
unified 5-field IME on the shared inbox (design.md). One adapter per source;
adding a source is a new module + one manifest line (zero core change)."""
