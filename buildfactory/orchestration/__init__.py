"""V7 deterministic control plane.

The package intentionally imports no submodules. Current entrypoints are the
Company Hub, resident Agent loop, Department provisioner, Worker Manager, and
ephemeral Verifier runtime. Import the concrete module a process needs.
"""
